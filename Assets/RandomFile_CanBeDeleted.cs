using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class RandomFile_CanBeDeleted : MonoBehaviour
{
    // Start is called before the first frame update
    // Random stuff
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
